changing zoom permanently
scroll in vbox_right
draw each X frame
save each X frame
'save screen' button
active/inactive (blind) controls
label on saving image (name and frame number)
grid on/off when zoom >= 3
invert colors
statusbar:
	frame count
	state (running/pause/saving image)
	'changes will take effect after restart'
hide/show vbox_right
adding own parameters
adding own hbox'es
field presets
fullscreen mode, switching by double click
buttons to save/load config
popup when incorrect values are set


possible own parameters:
	numbers: start, finish, step, default
	bool: default
	choice: list, default
